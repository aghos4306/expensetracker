//Reducer gives how we specify app state changes in response to certain actions to store, to //////context
export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
